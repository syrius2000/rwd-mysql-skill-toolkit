# Sample configs (manual / docs)

JSON fragments for local runs or documentation. Paths are relative to the repository root. Not all keys are valid for every skill; see each skill’s `SKILL.md` and `references/config-schema.md` where applicable.
