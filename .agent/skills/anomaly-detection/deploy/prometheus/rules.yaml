groups:
  - name: edc-rwd-anomaly-detection
    rules:
      - alert: HighAnomalyRate
        expr: anomaly_rate > 0.10
        for: 30m
        labels:
          severity: warning
        annotations:
          summary: "Anomaly rate is unusually high"
